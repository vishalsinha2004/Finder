setup

1.npx react-native init MyApp
2.npx @react-native-community/cli init MyApp
3.npx react-native start
4.npx react-native run-android

"
rename app.jsx into app.js
add logo on assets folder file is logo.png

"


topic 3 : login page without css

import React, { useState } from 'react';
import {
  SafeAreaView,
  View,
  Text,
  TextInput,
  StyleSheet,
  TouchableOpacity,
  Image
} from 'react-native';

const App = () => {

  const [username, setUsername] = useState('');
  const [password, setPassword] = useState('');

  return (
    <SafeAreaView style={styles.container}>

      {/* Title */}
      <Text style={styles.title}>Login Form</Text>

      {/* Profile Icon / Logo */}
      <Image
        source={require('./assets/logo.png')} // or use user icon
        style={styles.logo}
      />

      {/* Input Fields */}
      <View style={styles.inputContainer}>
        <TextInput
          placeholder="Enter User Name :"
          style={styles.input}
          value={username}
          onChangeText={setUsername}
        />

        <TextInput
          placeholder="Password :"
          secureTextEntry
          style={styles.input}
          value={password}
          onChangeText={setPassword}
        />
      </View>

      {/* Button */}
      <TouchableOpacity style={styles.button}>
        <Text style={styles.buttonText}>Login</Text>
      </TouchableOpacity>

    </SafeAreaView>
  );
};

export default App;

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#dcdcdc',
    alignItems: 'center',
    justifyContent: 'center'
  },

  title: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 20
  },

  logo: {
    width: 80,
    height: 80,
    borderRadius: 40,
    marginBottom: 25,
    borderWidth: 2,
    borderColor: '#333'
  },

  inputContainer: {
    width: '85%'
  },

  input: {
    backgroundColor: '#bbb',
    padding: 12,
    borderRadius: 8,
    marginVertical: 10
  },

  button: {
    backgroundColor: '#6c6c72',
    width: '85%',
    padding: 12,
    borderRadius: 8,
    marginTop: 20,
    alignItems: 'center'
  },

  buttonText: {
    color: '#fff',  
    fontWeight: 'bold'
  }
});
