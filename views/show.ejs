<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= filename %> | Finder</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
</head>
<body class="bg-zinc-900 text-gray-100 min-h-screen">
    <div class="container mx-auto p-4 md:p-10">
        <!-- Back button -->
        <a href="/" class="text-blue-400 hover:text-blue-300 inline-flex items-center mb-6 group transition-colors">
            <i class="fas fa-arrow-left mr-2 group-hover:-translate-x-1 transition-transform"></i>
            Go Back To Home
        </a>
        
        <!-- File content -->
        <div class="bg-zinc-800 rounded-lg shadow-xl overflow-hidden">
            <div class="p-6">
                <h1 class="text-2xl md:text-3xl font-bold mb-4 text-blue-400">
                    <%= filename %>
                </h1>
                
                <div class="prose prose-invert max-w-none">
                    <%# Render markdown content %>
                    <%- marked.parse(filedata) %>
                </div>
                
                <div class="mt-6 pt-4 border-t border-zinc-700 flex justify-end">
                    <a href="/edit/<%= filename %>" class="px-4 py-2 bg-blue-600 hover:bg-blue-700 rounded-md transition-colors">
                        <i class="fas fa-edit mr-2"></i> Edit File
                    </a>
                </div>
            </div>
        </div>
    </div>
</body>
</html>